import CountryCodePicker from './CountryCodePicker'

describe('@components/CountryCodePicker', () => {
  it('exports a valid component', () => {
    expect(CountryCodePicker).toBeAComponent()
  })
})
