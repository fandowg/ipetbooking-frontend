import OrderChangeStatusDialog from './OrderChangeStatusDialog'

describe('@components/OrderChangeStatusDialog', () => {
  it('exports a valid component', () => {
    expect(OrderChangeStatusDialog).toBeAComponent()
  })
})
