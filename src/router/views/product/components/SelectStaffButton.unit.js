import SelectStaffButton from './SelectStaffButton'

describe('@components/SelectStaffButton', () => {
  it('exports a valid component', () => {
    expect(SelectStaffButton).toBeAComponent()
  })
})
