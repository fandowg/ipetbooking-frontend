import InventoryListItem from './InventoryListItem'

describe('@components/InventoryListItem', () => {
  it('exports a valid component', () => {
    expect(InventoryListItem).toBeAComponent()
  })
})
