import Step2InventoryCalendar from './Step2InventoryCalendar'

describe('@components/Step2InventoryCalendar', () => {
  it('exports a valid component', () => {
    expect(Step2InventoryCalendar).toBeAComponent()
  })
})
